name: CI

on:
  push:
    branches: [main, master, captiveportal_oc, 14082026, 15082026]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build containers
        run: docker compose -p captiveportal_test -f docker-compose.yml -f docker-compose.test.yml up -d --build

      - name: Wait for services
        run: |
          sleep 15
           docker compose -p captiveportal_test -f docker-compose.yml -f docker-compose.test.yml ps

      - name: Run tests
        run: docker compose -p captiveportal_test -f docker-compose.yml -f docker-compose.test.yml exec -T portal npm test -- --coverage=false

      - name: Show logs on failure
        if: failure()
        run: docker compose -p captiveportal_test -f docker-compose.yml -f docker-compose.test.yml logs portal

      - name: Shutdown
        if: always()
        run: docker compose -p captiveportal_test -f docker-compose.yml -f docker-compose.test.yml down -v
